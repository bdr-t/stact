import { ThemeType } from './types';

const darkTheme: ThemeType = {
  bg: '#504E58',
  color: '#EDEDED',
};

const lightTheme: ThemeType = {
  color: '#504E58',
  bg: '#EDEDED',
};

export const theme = {
  light: lightTheme,
  dark: darkTheme,
};
